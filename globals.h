#ifndef GLOBALS_H
#define GLOBALS_H

#include <stdio.h>

extern FILE *output;   // Variável para o arquivo de saída
extern int yylineno;   // Linha atual do scanner
extern int yycolumn;   // Coluna atual do scanner

#endif // GLOBALS_H
